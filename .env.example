# GitHub OAuth Configuration
# Get these from: https://github.com/settings/developers
# Create a new OAuth App and copy the Client ID and Client Secret
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# NextAuth Configuration
# For production, set this to your production URL (e.g., https://yourdomain.com)
# For local development, use: http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Generate a random secret: openssl rand -base64 32
# Or use: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=your_nextauth_secret_here

# Ollama Configuration (Optional - only needed if using Ollama)
# Default: http://localhost:11434
# Only set this if you're using Ollama and it's running on a different URL
OLLAMA_API_URL=http://localhost:11434

# Public Ollama API URL (Optional - for client-side defaults)
# Default: http://localhost:11434
# Only set this if you need to override the default Ollama URL for client-side code
NEXT_PUBLIC_OLLAMA_API_URL=http://localhost:11434
